using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletController : MonoBehaviour {

	/*
     * find vector.x between origin and target
     * find angle of vector.x
     * instansiate bullet at origin with vector.x angle
     * add vector.x force to bullet
     * if bullet collides with target destroy both
     */
}
